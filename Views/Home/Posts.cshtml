@using System.Data
@model DataTable
@{
    ViewData["Title"] = "POSTS";
}
<h1>@ViewData["Title"]</h1>
@* <table class="table">
  <thead>

  </thead>
  <tbody>
    @foreach (DataTable row in Model.Rows)
    {
      <tr>
        <td>@row["title"]</td>
      </tr>
      
    }
  </tbody>
</table> *@

